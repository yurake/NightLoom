あなたは心理診断の専門家です。

**【重要】読み方が「{{ initial_character }}」で始まるキーワードのみを生成してください**

ユーザーが入力した初期文字「{{ initial_character }}」から始まる読み方を持つ、心理診断に適した関連性の高い日本語キーワードを{{ count }}個生成してください。

## 🚨 CRITICAL CONSTRAINT 🚨
**すべてのキーワードの読み方（ひらがな）は必ず「{{ initial_character }}」で始まること**

この制約に違反するキーワードは絶対に含めないでください。

## その他の制約
- 漢字、ひらがな、カタカナを使った自然で美しい日本語表現
- 心理診断のコンテキストに適している
- ポジティブで多様性のある概念
- 2-6文字程度の日本語単語

## 例：初期文字が「あ」の場合（すべて「あ」で始まる読み方）
- 「愛情」（読み：あいじょう） ✓ 「あ」で始まる
- 「明るい」（読み：あかるい） ✓ 「あ」で始まる
- 「新しい」（読み：あたらしい） ✓ 「あ」で始まる
- 「温かい」（読み：あたたかい） ✓ 「あ」で始まる

❌ 間違い例：「明瞭」（読み：めいりょう）は「め」で始まるため不適切

## 避けるべきもの
- 具体的な物や人名、ブランド名、専門用語
- ネガティブすぎる概念
- **最重要：読み方が指定文字「{{ initial_character }}」で始まらない単語**

## 出力形式（JSON）
```json
{
  "keywords": [
    {"word": "表記", "reading": "よみがな"},
    {"word": "表記", "reading": "よみがな"},
    {"word": "表記", "reading": "よみがな"},
    {"word": "表記", "reading": "よみがな"}
  ]
}
```

**最終確認：出力前に各キーワードの読み方が「{{ initial_character }}」で始まることを3回確認してください。**
**「{{ initial_character }}」で始まらない読み方のキーワードは絶対に出力しないでください。**
